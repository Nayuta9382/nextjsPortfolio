import { StaticImageData } from "next/image";

export type workData ={
    title : string,
    language : string,
    imgUrl : string,
    period : string,
    tool : string,
    explanation : string
};